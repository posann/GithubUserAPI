<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GithubUser" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light2</item>
        <item name="colorPrimaryVariant">@color/light1</item>
        <item name="colorOnPrimary">@color/light2</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/light2</item>
        <item name="android:navigationBarColor">@color/light2</item>
        <!-- Customize your theme here. -->

    </style>

    <style name="ThemeOverlay.GithubUser.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>